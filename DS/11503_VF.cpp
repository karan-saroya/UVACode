#include<stdio.h>
#include<stdlib.h>
#define MAX 100000
int p[MAX];
int size[MAX];
char value[MAX][32];
int main()
{
	int num_cases;
	scanf("%d",&num_cases);
	int num_el;
	scanf("%d",&num_el);
	for(int) 
	
}
void make_set(int j)
{
	p[j]=j;
	size[j]=1;
}
void find(int )
{
	
}

