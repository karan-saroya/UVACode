#include<stdio.h>
#include<stdlib.h>
#include<math.h>
int *sieve;
int main()
{
	unsigned long num;
	scanf("%lu",&num);
	unsigned long root= sqrt(num);
	soe(root);
	sieve= (int *)malloc(sizeof());
	
	return 0;
}


